- operation: run (shell) command
- context struct instead of passing config directly to command dispatchers
- registry as variable instead of global static
- configuring secrets and using env vars for operations
- improve op documentation
- move logic from cli to core
- remove 'dependency' feature
- rework cli structure
   - plumbing
     - ic resolve [fact id...]
     - ic diff <snapshot before> < <snapshot after>
     - ic apply <snapshot before> [fact id...] < <snapshot after>
   - porcelain
     - ic init
     - ic add <fact id>
     - ic show <fact id>
     - ic edit <fact id>
     - ic remove <fact id>
     - ic list
     - ic step add <fact id> <operation id> -- --option value
     - ic step remove <fact id> <index>
     - ic step list <fact id>
     - ic snapshot baseline [fact id...]
     - ic snapshot candidate [fact id...]
     - ic audit [fact id...]
     - ic inspect [fact id...]
     - ic accept <fact id...>
- remove fact scoping
- show fact description in diffs
- remove fact TTL?
- use only one batch while working through a pipeline, instead of grouping
- github action

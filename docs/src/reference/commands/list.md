# `ic list`

List indexed facts.

## Syntax

```bash
ic list [--verbose]
```

## Options

- `--verbose`
  Print `label: description` instead of just the label.

## Notes

- Only indexed facts are listed.
- The output order is stable and sorted by label.

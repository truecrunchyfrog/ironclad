# Guide

This guide walks through the practical use of Ironclad.

Use `ic --help` and `ic <command> --help` for command syntax.
This section focuses on how the pieces fit together when you are building and maintaining a real catalog.

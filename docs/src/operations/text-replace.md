# `text.replace`

Replace text inside each sample.

## Options

```toml
text = ""
regex = ""
replacement = ""
max = 0
```

- choose either `text` or `regex`
- `replacement`
  replacement string
- `max`
  optional maximum replacement count
